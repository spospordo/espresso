// config.js
const config = {
    serverURL: 'http://localhost:3000/get-text',  // Server URL for fetching text values
    imagePaths: {
        smallContainer: './smallContainer.png',  // Local path for smallContainer.png
        mediumContainer: './mediumContainer.png',  // Local path for mediumContainer.png
        largeContainer: './largeContainer.png'   // Local path for largeContainer.png
    }
};

export default config;